<p id="notice"><%= notice %></p>

<h1>Messages</h1>

<%= render 'form', message: Message.new %>

<table>
  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= message.text %></td>
      </tr>
    <% end %>
  </tbody>
</table>
